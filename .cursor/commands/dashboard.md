Regenerate the family dashboard following dashboard/README.md exactly: build
the data object from the current personal files (config, profile, inbox,
calendar-cadences, enabled systems/ files, logs), replace the block between
/*FAMILY-OS-DATA-START*/ and /*FAMILY-OS-DATA-END*/ in dashboard/template.html
with the real data (demo: false), and write dashboard/dashboard.html. Then
tell the user to open it — or refresh the tab if it's already open. Data must
reflect only what's truly in the files; empty sections are fine.
